res = Square(x)
    res = x * x

res, prod = SumAndProduct(a, b)
    res = a + b
    prod = a * b

res = BiasAndScale(x, bias, scale)
    tmp = x + bias
    res = tmp * scale

res = Rebuild(i)
    res = [i + 1]
