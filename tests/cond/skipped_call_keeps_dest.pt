# Outputs written only when the condition holds, so a call to either may leave
# the caller's destination untouched.
res = KeepScalar(x)
    res = x > 0 42

res = KeepArray(x)
    res = x > 0 [7 8]

res = KeepRangedArray(i)
    res = i < 0 [i]
