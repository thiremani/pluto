y = Triple(x)
    tripled = 3x
    tripled
    y = tripled

res = Sum(curr, x)
    total = curr + x
    total
    res = total

yes = Divides(in, y, x)
    yes = in * (y % x)

# ret = primes(num)
#    i = 2:num
#    j = 2:i/2
#    yes = 1
#    yes = divides(yes, i, j)
#    ret = "Is not a prime"
#    ret = yes != 0 "Is a prime"
#    "-num -isPrime"