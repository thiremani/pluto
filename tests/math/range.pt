y = Triple(x)
    y = 3x
    y

res = Sum(curr, x)
    res = curr + x
    res

yes = Divides(in, y, x)
    yes = in * (y % x)

# ret = primes(num)
#    i = 2:num
#    j = 2:i/2
#    yes = 1
#    yes = divides(yes, i, j)
#    ret = "Is not a prime"
#    ret = yes != 0 "Is a prime"
#    "-num -isPrime"