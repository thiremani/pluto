res = Id(x)
    res = x

res = AccViaId(a, x)
    tmp = Id(a)
    res = tmp + x

res = Acc(a, x)
    res = a + x

res = ConditionalAcc(a, x)
    res = x > 0 a + x

sum, last = AccPair(a, x)
    sum = a + x
    last = x
