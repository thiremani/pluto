# A heap-string argument whose same-named destination receives a static output.
# The output uses the destination's heap storage, so the later input read
# observes the replacement even though its expression produces a static string.
out, echo = RefineEcho(s, x)
    out = "static"
    echo = x > -1 s ⊕ "!"
